<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tipsy.app"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:debuggable="true"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar.White"
            android:launchMode="singleTop"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HelpActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ChoiceActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".orga.SignUpOrgaActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:noHistory="true"></activity>

        <activity
            android:name=".membre.SignUpMembreActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:noHistory="true"></activity>

        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" /></activity>

        <activity
            android:name=".membre.MembreActivity"
            android:noHistory="true"></activity>

        <activity
            android:name=".orga.HomeOrgaActivity"
            android:noHistory="true"></activity>

        <activity android:name=".orga.EditEventActivity"></activity>

    </application>

</manifest>